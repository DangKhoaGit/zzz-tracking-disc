<Window x:Class="ZZZBuffTracker.App.OverlayWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:ZZZBuffTracker.App"
 Title="ZZZ Buff Tracker Overlay" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
 ShowInTaskbar="False" Topmost="True" ShowActivated="False" ResizeMode="NoResize"
 MinWidth="160" MinHeight="100" MaxWidth="4000" MaxHeight="4000">
 <Window.Resources><BooleanToVisibilityConverter x:Key="Visible"/></Window.Resources>
 <Border x:Name="Frame" BorderThickness="1" CornerRadius="8" Padding="4">
  <Border.Style><Style TargetType="Border">
   <Setter Property="BorderBrush" Value="Transparent"/><Setter Property="Background" Value="Transparent"/>
   <Style.Triggers><DataTrigger Binding="{Binding IsEditing}" Value="True">
    <Setter Property="BorderBrush" Value="#91E4BC"/><Setter Property="Background" Value="#EB12161E"/>
   </DataTrigger></Style.Triggers>
  </Style></Border.Style>
  <Grid>
   <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <Border Background="#283B42" Padding="8" Cursor="SizeAll" MouseLeftButtonDown="DragHeader"
    Visibility="{Binding IsEditing, Converter={StaticResource Visible}}">
    <TextBlock Text="EDIT · Kéo để di chuyển" Foreground="White" FontWeight="Bold"/>
   </Border>
   <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden"
    IsHitTestVisible="{Binding IsEditing}">
    <local:OverlayContent/>
   </ScrollViewer>
   <DockPanel Grid.Row="2" Visibility="{Binding IsEditing, Converter={StaticResource Visible}}">
    <Thumb Width="22" Height="22" DockPanel.Dock="Right" HorizontalAlignment="Right" Cursor="SizeNWSE" DragDelta="ResizeOverlay"
     AutomationProperties.Name="Resize overlay"/>
    <Button Content="Lưu &amp; Play" Command="{Binding SaveCommand}" Padding="8,4" HorizontalAlignment="Left"/>
   </DockPanel>
  </Grid>
 </Border>
</Window>
